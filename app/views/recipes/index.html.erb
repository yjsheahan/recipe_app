<div class="index-container">
  <div class="hero">
    <h1>
      Vegan
      <br>
      <span>Bakes.</span>
    </h1>
    <% if user_signed_in? %>
      <h6>Welcome back!</h6>
    <% else %>
      <h6>A place to exchange tried and tested vegan baking recipes!</h6>
    <% end %>
    <div class="hero-circle">
      <p>Scroll for recipes...</p>
    </div>

    <!-- TODO Get image showing and reduce transparency -->
    <div class="cupcake-home" style="background-image: url('https://res.cloudinary.com/dpzjvi2w1/image/upload/v1596998123/cupcake-home_opeszq.jpg');">
    </div>
  </div>

  <div class="recipes-index-container">
    <h1>Plant Based Treats.</h1>

    <div class="recipes-container">
      <% @recipe.each_slice(3) do |recipes| %>
        <div class="row">
          <% recipes.each do |recipe| %>
            <div class="col">
              <div class="recipe">
                <div class="image-wrapper">
                  <%= link_to recipe do %>
                    <%= cl_image_tag recipe.image.key, width: 360, height: 320, crop: :fill %>
                    <h2><%= link_to recipe.title, recipe_path(recipe) %></h2>
                  <% end %>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
    <div class="bottom-row">
      <%= link_to "Edit/Delete Account", edit_user_registration_path %>
    </div>
  </div>
</div>

